# BOPS-Applicants Docker image for NDX:Try
# Built from the upstream BOPS-Applicants Dockerfile.production with overlay files
#
# This Dockerfile is NOT used directly — see docker/bops/Dockerfile for explanation.
# The GitHub Actions workflow clones bops-applicants, copies overlays, and builds.
#
# To build locally:
#   ./build.sh
