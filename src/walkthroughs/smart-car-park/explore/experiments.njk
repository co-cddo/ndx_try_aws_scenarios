---
layout: layouts/exploration
title: "Experiments - Smart Car Park"
description: "5 guided experiments with sensors and real-time dashboards"
scenarioId: smart-car-park
eleventyNavigation:
  key: smart-car-park-experiments
  parent: smart-car-park-explore
  title: Experiments
---

{# Story 9.2: Smart Car Park Experiments Page #}

{% set scenarioId = "smart-car-park" %}
{% set explorationData = exploration[scenarioId] %}
{% set activities = explorationData.activities %}

{% set experiments = [] %}
{% for activity in activities %}
  {% if activity.category == "experiments" %}
    {% set experiments = (experiments.push(activity), experiments) %}
  {% endif %}
{% endfor %}

<section class="ndx-experiments-intro" aria-labelledby="intro-heading">
  <h2 class="govuk-heading-l" id="intro-heading">IoT Dashboard Experiments</h2>
  <p class="govuk-body-l">
    {{ experiments | length }} guided experiments to understand how real-time IoT
    data flows from sensors to dashboards.
  </p>

  {# Sample Data Explanation Panel (Story 12.2) - AC1, AC2, AC3 #}
  {% include "components/sample-data-panel.njk" %}

  <div class="govuk-inset-text">
    <strong>What you'll learn:</strong> How sensor state changes propagate,
    dashboard update patterns, and alert threshold behavior.
  </div>
  <div class="ndx-experiments-progress" data-progress-container>
    <p class="govuk-body">
      <strong>Experiments completed:</strong>
      <span data-completed-count>0</span> of {{ experiments | length }}
    </p>
    <div class="ndx-progress-bar" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="{{ experiments | length }}">
      <div class="ndx-progress-bar__fill" style="width: 0%"></div>
    </div>
  </div>
</section>

<section class="ndx-experiments-list" aria-labelledby="experiments-heading">
  <h2 class="govuk-visually-hidden" id="experiments-heading">Available experiments</h2>
  {% for experiment in experiments %}
    {% set exp = experiment %}
    {% include "components/exploration/experiment-card.njk" %}
  {% endfor %}
</section>

<section class="ndx-experiments-next" aria-labelledby="next-heading">
  <h2 class="govuk-heading-l" id="next-heading">Next steps</h2>
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-one-half">
      <div class="ndx-next-card">
        <h3 class="govuk-heading-s">Architecture Tour</h3>
        <p class="govuk-body-s">Understand the full IoT data pipeline.</p>
        <a href="/walkthroughs/{{ scenarioId }}/explore/architecture/" class="govuk-link">Take the tour</a>
      </div>
    </div>
    <div class="govuk-grid-column-one-half">
      <div class="ndx-next-card">
        <h3 class="govuk-heading-s">Test the Limits</h3>
        <p class="govuk-body-s">What happens when sensors fail?</p>
        <a href="/walkthroughs/{{ scenarioId }}/explore/limits/" class="govuk-link">Try challenges</a>
      </div>
    </div>
  </div>
</section>

<style>
  .ndx-experiments-intro { margin-bottom: 40px; }
  .ndx-experiments-progress { background: #f8f8f8; padding: 20px; margin-top: 20px; }
  .ndx-progress-bar { height: 8px; background: #b1b4b6; border-radius: 4px; overflow: hidden; margin-top: 10px; }
  .ndx-progress-bar__fill { height: 100%; background: #00703c; transition: width 0.3s ease; }
  .ndx-experiments-next { margin-top: 60px; padding-top: 40px; border-top: 4px solid #1d70b8; }
  .ndx-next-card { padding: 20px; background: #f8f8f8; height: 100%; }
  @media (max-width: 768px) { .ndx-next-card { margin-bottom: 20px; } }
</style>
