---
layout: page
title: Schedule a Partner Tour
description: Request a guided evaluation tour led by an experienced AWS implementation partner
---

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-xl">Schedule a partner tour</h1>
    <p class="govuk-body-l">
      Request a guided 30-60 minute evaluation session with one of our vetted AWS implementation partners.
    </p>
  </div>
</div>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    <div class="govuk-inset-text">
      <p class="govuk-body">
        <strong>What to expect:</strong> An implementation partner will contact you within 3 working days to schedule your session. Tours are free of charge with no commitment required.
      </p>
    </div>

    {# Form with mailto: action for MVP - AC-2.6.4 #}
    <form id="partner-tour-form" class="ndx-partner-tour-form" action="#" method="post" novalidate>

      {# Organisation Name - Required - AC-2.6.2, AC-2.6.3 #}
      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--m" for="council-name">
          Organisation name
          <span class="ndx-required" aria-hidden="true">*</span>
        </label>
        <span id="council-name-hint" class="govuk-hint">
          The name of your local authority
        </span>
        <input class="govuk-input" id="council-name" name="councilName" type="text"
               aria-describedby="council-name-hint" aria-required="true" required>
      </div>

      {# Contact Name - Required - AC-2.6.2, AC-2.6.3 #}
      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--m" for="contact-name">
          Your name
          <span class="ndx-required" aria-hidden="true">*</span>
        </label>
        <input class="govuk-input" id="contact-name" name="contactName" type="text"
               autocomplete="name" aria-required="true" required>
      </div>

      {# Email - Required - AC-2.6.2, AC-2.6.3 #}
      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--m" for="email">
          Email address
          <span class="ndx-required" aria-hidden="true">*</span>
        </label>
        <span id="email-hint" class="govuk-hint">
          We'll use this to arrange your partner tour session
        </span>
        <input class="govuk-input" id="email" name="email" type="email"
               autocomplete="email" spellcheck="false"
               aria-describedby="email-hint" aria-required="true" required>
      </div>

      {# Phone - Optional - AC-2.6.2 #}
      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--m" for="phone">
          Phone number
          <span class="govuk-hint govuk-!-display-inline">(optional)</span>
        </label>
        <span id="phone-hint" class="govuk-hint">
          For the partner to call you directly if preferred
        </span>
        <input class="govuk-input govuk-input--width-20" id="phone" name="phone" type="tel"
               autocomplete="tel" aria-describedby="phone-hint">
      </div>

      {# Scenario Interest - Required - AC-2.6.2, AC-2.6.3 #}
      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--m" for="scenario-interest">
          Scenario of interest
          <span class="ndx-required" aria-hidden="true">*</span>
        </label>
        <span id="scenario-interest-hint" class="govuk-hint">
          Select the scenario you'd like to explore during your tour
        </span>
        <select class="govuk-select" id="scenario-interest" name="scenarioInterest"
                aria-describedby="scenario-interest-hint" aria-required="true" required>
          <option value="">Select a scenario</option>
          <option value="council-chatbot">Council Chatbot - AI-powered resident Q&A</option>
          <option value="planning-ai">Planning Application AI - Document extraction</option>
          <option value="foi-redaction">FOI Redaction - Automated redaction assistant</option>
          <option value="smart-car-park">Smart Car Park - IoT occupancy monitoring</option>
          <option value="text-to-speech">Text to Speech - Accessible content generation</option>
          <option value="quicksight-dashboard">QuickSight Dashboard - Analytics visualisation</option>
          <option value="multiple">Multiple scenarios - I'd like to discuss options</option>
        </select>
      </div>

      {# Available Times - Required - AC-2.6.2, AC-2.6.3 #}
      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--m" for="available-times">
          Preferred availability
          <span class="ndx-required" aria-hidden="true">*</span>
        </label>
        <span id="available-times-hint" class="govuk-hint">
          Let us know your general availability for a 30-60 minute session (e.g., "Mornings Tue-Thu" or "Any afternoon next week")
        </span>
        <textarea class="govuk-textarea" id="available-times" name="availableTimes" rows="3"
                  aria-describedby="available-times-hint" aria-required="true" required></textarea>
      </div>

      {# Additional Context - Optional - AC-2.6.2 #}
      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--m" for="additional-context">
          Additional information
          <span class="govuk-hint govuk-!-display-inline">(optional)</span>
        </label>
        <span id="additional-context-hint" class="govuk-hint">
          Any specific questions, requirements, or context you'd like the partner to know beforehand
        </span>
        <textarea class="govuk-textarea" id="additional-context" name="additionalContext" rows="5"
                  aria-describedby="additional-context-hint"></textarea>
      </div>

      {# GDPR Privacy Notice - AC-2.6.6 #}
      <div class="govuk-form-group">
        <fieldset class="govuk-fieldset">
          <legend class="govuk-fieldset__legend govuk-fieldset__legend--m">
            Privacy and data protection
          </legend>
          <div class="govuk-checkboxes govuk-checkboxes--small" data-module="govuk-checkboxes">
            <div class="govuk-checkboxes__item">
              <input class="govuk-checkboxes__input" id="privacy-consent" name="privacyConsent"
                     type="checkbox" value="agreed" aria-required="true" required>
              <label class="govuk-label govuk-checkboxes__label" for="privacy-consent">
                I agree to my details being shared with an AWS implementation partner to arrange this tour session
                <span class="ndx-required" aria-hidden="true">*</span>
              </label>
            </div>
          </div>
        </fieldset>
        <details class="govuk-details govuk-!-margin-top-2" data-module="govuk-details">
          <summary class="govuk-details__summary">
            <span class="govuk-details__summary-text">
              How we use your information
            </span>
          </summary>
          <div class="govuk-details__text">
            <p>Your information will be:</p>
            <ul class="govuk-list govuk-list--bullet">
              <li>Shared only with the partner assigned to your tour request</li>
              <li>Used solely to arrange and conduct your evaluation session</li>
              <li>Not used for marketing purposes without additional consent</li>
              <li>Retained for up to 12 months for service improvement</li>
            </ul>
            <p>
              See our <a href="/privacy/" class="govuk-link">privacy policy</a> for full details on data handling.
            </p>
          </div>
        </details>
      </div>

      {# Required fields note - AC-2.6.3 #}
      <p class="govuk-body-s govuk-!-margin-bottom-4">
        Fields marked with <span class="ndx-required" aria-hidden="true">*</span> are required.
      </p>

      {# Error Summary - hidden initially, shown on validation failure #}
      <div class="govuk-error-summary ndx-partner-tour-form__error-summary" aria-labelledby="error-summary-title"
           role="alert" tabindex="-1" data-module="govuk-error-summary" hidden>
        <h2 class="govuk-error-summary__title" id="error-summary-title">
          There is a problem
        </h2>
        <div class="govuk-error-summary__body">
          <ul class="govuk-list govuk-error-summary__list" id="error-summary-list">
            {# Error messages inserted by JavaScript #}
          </ul>
        </div>
      </div>

      {# Submit Button #}
      <div class="govuk-button-group">
        <button type="submit" class="govuk-button" data-module="govuk-button">
          Request partner tour
        </button>
        <a href="/scenarios/" class="govuk-link">Cancel and return to scenarios</a>
      </div>

    </form>

    {# Confirmation Message - shown after successful submission - AC-2.6.5 #}
    <div id="confirmation-message" class="ndx-partner-tour-form__confirmation" hidden>
      <div class="govuk-panel govuk-panel--confirmation">
        <h2 class="govuk-panel__title">
          Tour request submitted
        </h2>
        <div class="govuk-panel__body">
          Thank you for your request
        </div>
      </div>

      <h3 class="govuk-heading-m govuk-!-margin-top-6">What happens next</h3>
      <p class="govuk-body">
        An implementation partner will contact you within <strong>3 working days</strong> to:
      </p>
      <ul class="govuk-list govuk-list--bullet">
        <li>Confirm your preferred time slot</li>
        <li>Send calendar invitations and meeting details</li>
        <li>Provide any preparation materials</li>
      </ul>

      <p class="govuk-body">
        <a href="/scenarios/" class="govuk-link">Return to scenarios</a> or
        <a href="/" class="govuk-link">go to the homepage</a>.
      </p>
    </div>

  </div>

  {# Sidebar with benefits #}
  <div class="govuk-grid-column-one-third">
    <div class="ndx-partner-tour-sidebar">
      <h2 class="govuk-heading-s govuk-!-margin-bottom-3">Partner tour benefits</h2>
      <ul class="govuk-list govuk-list--bullet govuk-body-s">
        <li>No AWS account needed</li>
        <li>Expert guidance throughout</li>
        <li>Ask questions in real-time</li>
        <li>Understand procurement options</li>
        <li>Free with no commitment</li>
      </ul>

      <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">

      <h3 class="govuk-heading-s govuk-!-margin-bottom-3">Prefer to try yourself?</h3>
      <p class="govuk-body-s">
        <a href="/scenarios/" class="govuk-link">Browse scenarios</a> to deploy directly in your Innovation Sandbox account.
      </p>
    </div>
  </div>
</div>

{# JavaScript for form handling - progressive enhancement (works without JS via mailto) #}
<script>
(function() {
  'use strict';

  var form = document.getElementById('partner-tour-form');
  var confirmationMessage = document.getElementById('confirmation-message');
  var errorSummary = document.querySelector('.ndx-partner-tour-form__error-summary');
  var errorSummaryList = document.getElementById('error-summary-list');

  if (!form || !confirmationMessage) return;

  // Get scenario from URL if present
  var urlParams = new URLSearchParams(window.location.search);
  var scenarioParam = urlParams.get('scenario');
  if (scenarioParam) {
    var scenarioSelect = document.getElementById('scenario-interest');
    if (scenarioSelect) {
      for (var i = 0; i < scenarioSelect.options.length; i++) {
        if (scenarioSelect.options[i].value === scenarioParam) {
          scenarioSelect.selectedIndex = i;
          break;
        }
      }
    }
  }

  form.addEventListener('submit', function(event) {
    event.preventDefault();

    // Clear previous errors
    clearErrors();

    // Validate form
    var errors = validateForm();

    if (errors.length > 0) {
      showErrors(errors);
      return;
    }

    // Build mailto link with form data
    var formData = new FormData(form);
    var subject = encodeURIComponent('Partner Tour Request: ' + formData.get('councilName'));
    var body = buildEmailBody(formData);

    // For MVP: open mailto link
    var mailtoLink = 'mailto:ndx@dsit.gov.uk?subject=' + subject + '&body=' + body;

    // Show confirmation (user will also see mailto client open)
    form.hidden = true;
    confirmationMessage.hidden = false;
    confirmationMessage.focus();

    // Open mail client
    window.location.href = mailtoLink;
  });

  function validateForm() {
    var errors = [];
    var requiredFields = [
      { id: 'council-name', label: 'Council name' },
      { id: 'contact-name', label: 'Your name' },
      { id: 'email', label: 'Email address' },
      { id: 'scenario-interest', label: 'Scenario of interest' },
      { id: 'available-times', label: 'Preferred availability' },
      { id: 'privacy-consent', label: 'Privacy consent', isCheckbox: true }
    ];

    requiredFields.forEach(function(field) {
      var element = document.getElementById(field.id);
      if (!element) return;

      var isEmpty = field.isCheckbox ? !element.checked : !element.value.trim();

      if (isEmpty) {
        errors.push({
          id: field.id,
          message: field.isCheckbox
            ? 'You must agree to the privacy terms'
            : 'Enter ' + field.label.toLowerCase()
        });
      }
    });

    // Validate email format
    var emailField = document.getElementById('email');
    if (emailField && emailField.value.trim()) {
      var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailRegex.test(emailField.value.trim())) {
        // Remove any existing email error and add format error
        errors = errors.filter(function(e) { return e.id !== 'email'; });
        errors.push({
          id: 'email',
          message: 'Enter an email address in the correct format, like name@example.com'
        });
      }
    }

    return errors;
  }

  function showErrors(errors) {
    errorSummaryList.innerHTML = '';

    errors.forEach(function(error) {
      // Add to error summary
      var li = document.createElement('li');
      var link = document.createElement('a');
      link.href = '#' + error.id;
      link.textContent = error.message;
      li.appendChild(link);
      errorSummaryList.appendChild(li);

      // Add error state to field
      var field = document.getElementById(error.id);
      if (field) {
        var formGroup = field.closest('.govuk-form-group');
        if (formGroup) {
          formGroup.classList.add('govuk-form-group--error');

          // Add error message above field
          var existingError = formGroup.querySelector('.govuk-error-message');
          if (!existingError) {
            var errorMsg = document.createElement('p');
            errorMsg.className = 'govuk-error-message';
            errorMsg.id = error.id + '-error';
            errorMsg.innerHTML = '<span class="govuk-visually-hidden">Error:</span> ' + error.message;

            // Insert before the input/select/textarea
            var input = formGroup.querySelector('input, select, textarea');
            if (input) {
              input.parentNode.insertBefore(errorMsg, input);
              input.setAttribute('aria-describedby',
                (input.getAttribute('aria-describedby') || '') + ' ' + errorMsg.id);
            }
          }
        }

        // Add error class to input
        if (field.classList.contains('govuk-input')) {
          field.classList.add('govuk-input--error');
        } else if (field.classList.contains('govuk-select')) {
          field.classList.add('govuk-select--error');
        } else if (field.classList.contains('govuk-textarea')) {
          field.classList.add('govuk-textarea--error');
        }
      }
    });

    errorSummary.hidden = false;
    errorSummary.focus();
  }

  function clearErrors() {
    errorSummary.hidden = true;
    errorSummaryList.innerHTML = '';

    // Remove error states from all fields
    var errorGroups = form.querySelectorAll('.govuk-form-group--error');
    errorGroups.forEach(function(group) {
      group.classList.remove('govuk-form-group--error');
      var errorMsg = group.querySelector('.govuk-error-message');
      if (errorMsg) {
        errorMsg.remove();
      }
    });

    var errorInputs = form.querySelectorAll('.govuk-input--error, .govuk-select--error, .govuk-textarea--error');
    errorInputs.forEach(function(input) {
      input.classList.remove('govuk-input--error', 'govuk-select--error', 'govuk-textarea--error');
    });
  }

  function buildEmailBody(formData) {
    var lines = [
      'Partner Tour Request',
      '==================',
      '',
      'Council: ' + formData.get('councilName'),
      'Contact: ' + formData.get('contactName'),
      'Email: ' + formData.get('email'),
      'Phone: ' + (formData.get('phone') || 'Not provided'),
      '',
      'Scenario: ' + formData.get('scenarioInterest'),
      '',
      'Availability:',
      formData.get('availableTimes'),
      '',
      'Additional Context:',
      formData.get('additionalContext') || 'None provided',
      '',
      '---',
      'Submitted via NDX:Try Partner Tour Form'
    ];

    return encodeURIComponent(lines.join('\n'));
  }
})();
</script>
